# Dictionary Mode

`sdgglyphs --dictionary`

A scrollable window showing all known glyphs as cards. Each card displays:

- Glyph **name**
- **Output string**
- Animated **stroke preview** — the path is repeatedly traced with a drawing head

The dictionary uses your GTK theme colours automatically.

Press `Escape` to close.
