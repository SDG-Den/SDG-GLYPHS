# docs review

README: dependencies: python-gobject instead of python3-gobject, same for tesseract-ocr, it's just tesseract and python3-cairo is just python-cairo.

README: OCR bracket mode: remove from docs, brackets are not brackets atm anyways, create TODO to fix this and re-add to docs. 

quick start: should mention being able to start the application from your desktop shell/runner as the primary entrypoint, including being able to right-click it to enter alternate modes.

normal-mode - should probably be renamed to main-interface. 

normal-mode: session execution: remove mention of the horizontal line, it is deprecated. 

recording: should mention how glyphs are stored and how to add/remove/share glyphs by sharing the file.

TODO: add todo with option to pass an optional --dictpath argument to sdgglyphs cli to make it read glyphs from a different location. TODO should be in the root of the repository.

tips: "no need to reach for a horizontal line" makes absolutely no sense here.

